app.js说明:
    是angular的启动文件,里面会初始化关于框架需要的一些组件和特性.在发布时文件会被压缩,这个文件不需要任何的修改,属于框架级别的.

main.js说明:
    是针对框架的配置和一些依赖项的配置.

main.css说明:
    是一些框架样式.

在build时,app.js,main.css需要在每次编译时复制.main.js只有在第一次build时才复制